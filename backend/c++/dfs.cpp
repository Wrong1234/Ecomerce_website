#include<bits/stdc++.h>
using namespace std';

void solve(){
    int n;
    cin >> n;

    vector<int>adj[n + 1];
    for(int i = 0; i < n; i++){
        int x, y;
        cin >> x >> y;

        adj[x].push_back(y);
        adj[y]
    }
}
int main(){

    int t;
    scanf("%d", &t);

    while(t--) solve();

}